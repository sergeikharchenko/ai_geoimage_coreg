torch>=1.10.0
torchvision>=0.11.0
numpy>=1.20.0
opencv-python>=4.5.0
rasterio>=1.2.0
pandas>=1.3.0
geopandas>=0.10.0
shapely>=1.8.0
kornia>=0.6.0
matplotlib>=3.4.0
scipy>=1.7.0

# GDAL is strictly required for this package.
# However, installing it via pip often fails without system headers.
# It is recommended to install GDAL via Conda: `conda install gdal`
# or ensure system libraries are present before uncommenting below:
# gdal>=3.0.0